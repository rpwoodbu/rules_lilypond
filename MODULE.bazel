module(name = "rules_lilypond", version = "0.0.3")

bazel_dep(name = "bazel_lib", version = "3.2.2")
bazel_dep(name = "rules_shell", version = "0.8.0")

lilypond = use_extension("//:extensions.bzl", "lilypond")

use_repo(lilypond, "lilypond")
